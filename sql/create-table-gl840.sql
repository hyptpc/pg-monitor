CREATE TABLE gl840
(timestamp    TIMESTAMP WITH TIME ZONE,
 ip_address   TEXT,
 channel      INTEGER,
 channel_name TEXT,
 value	      REAL,
 unit	      TEXT
);
 -- PRIMARY KEY (timestamp, channel));
