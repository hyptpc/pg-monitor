CREATE TABLE gl840
(ip_address   TEXT,
 timestamp    TIMESTAMP WITH TIME ZONE,
 channel      INTEGER,
 channel_name TEXT,
 value	      REAL,
 unit	      TEXT
);
 -- PRIMARY KEY (timestamp, channel));
